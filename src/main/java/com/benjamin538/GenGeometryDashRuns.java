package com.benjamin538;

import com.benjamin538.util.Logging;

public class GenGeometryDashRuns {
    public void generate() {
        Logging logger = new Logging();
        logger.warn("Since geometry dash binaries is not located on robtop website, program will use external source.");
    }
}